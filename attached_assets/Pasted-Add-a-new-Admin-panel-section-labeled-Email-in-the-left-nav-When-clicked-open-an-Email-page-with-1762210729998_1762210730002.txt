Add a new Admin panel section labeled “Email” in the left nav. When clicked, open an Email page with three features: (A) Change Bad Blue Support Email, (B) Recent User Logins (Newest → Oldest), and (C) Click-to-Email with full attachments. For (A), build editable fields for Support From Name and Support From Email with Save and Send Test Email buttons. Validate: From Name 1–80 chars; From Email RFC5322-valid; warn (non-blocking) if domain differs from our sending domain. Persist settings in a durable store (preferred: DB table app_settings with keys support_from_name and support_from_email; fallback: JSON at /config/app_settings.json). Create admin-only APIs: GET /admin/api/support-email returns { fromName, fromEmail }; POST /admin/api/support-email accepts { fromName, fromEmail }, validates, saves, returns updated values; POST /admin/api/support-email/test sends a sample to the logged-in admin using stored settings. Update the SMTP mailer to dynamically build the From header from stored settings with env-var fallback DEFAULT_FROM_NAME and DEFAULT_FROM_EMAIL; keep transport on Google Workspace SMTP relay (smtp-relay.gmail.com:587, STARTTLS) with env vars GWSMTP_USER and GWSMTP_PASS; never expose creds. Add audit logging on settings changes (adminId, old→new, timestamp). For (B), show a paginated table “Recent User Logins” with columns: User ID, Name, Email, Last Login (local time), Status. Sort strictly by last_login_at descending. Highlight any user with an active access subscription in blue (row background or left accent bar, accessible contrast). Provide search (by name/email) and quick filters All / Subscribers / Non-Subscribers. Data source: users table (id, name, email, access_active, last_login_at) or join latest from a login events table; API: admin-only GET /admin/api/users/logins?query=&filter=&page=&limit= returns newest→oldest; default 25/page. Show only necessary fields; never reveal passwords/tokens. For (C), make each email in the table clickable; clicking opens an Email Composer modal targeted to that user. Composer fields: To (prefilled, read-only), From (prefilled from stored Support From settings), Subject, Message (rich-text allowed), and Attachments with both “Choose files” and drag-and-drop. Show a file list with filename, type, size, and Remove control; allow multiple files; display total size and disable Send if limits exceeded. Accept safe types: pdf, doc, docx, xls, xlsx, csv, txt, jpg, jpeg, png, gif, mp4. Reject executables/archives (exe, dll, bin, sh, bat, cmd, msi, js, jar, zip, rar, 7z) or unknown MIME. Add optional template dropdown (General Support, Password Reset Instructions, Subscription Reminder, Thank You for Subscribing). Provide a subtle “Subscriber” badge in the composer header if the user is highlighted as a subscriber. Implement admin-only POST /admin/api/email/send as a multipart endpoint that accepts to, subject, html (and optional text), plus attachments[]; use a robust multipart parser (multer/busboy). Limits: per-file max 10 MB, total payload max 25 MB, max 10 files. Sanitize filenames, enforce MIME allow-list, and delete any temp files after send (success or failure). On send, the server fetches current Support From settings, builds From as “<fromName> <fromEmail>”, sends via the existing SMTP transport, and returns JSON with messageId, accepted, rejected, or a concise error (e.g., type invalid, size exceeded, SMTP failure). Enforce overall email size ≤ ~25 MB after MIME; return a friendly error if exceeded. Validation: To must be a valid internal user email; Subject and Message required; sanitize HTML (allow p, b, i, a, ul, ol, li, br). Add per-admin rate limiting (e.g., 30 sends/hour) and CSRF protection where applicable. Log every send attempt with adminId, userId, to, timestamp, subject hash (not full content), attachment count/total size, and SMTP messageId. Security: all /admin/api/* endpoints require authenticated admin with proper role/permission; never expose SMTP creds; only minimal PII is shown. UX: show success/error toasts for Save, Test, and Send; keep Send disabled until fields are valid and size limits respected; include pagination controls for the user list. Deliverables: new Email nav and page with (A) support email settings + test, (B) recent logins table (newest→oldest, subscribers in blue, search/filters/pagination), and (C) click-to-email composer with full attachments and templates; updated mailer using dynamic From; validation, sanitization, rate limits, error handling, temp-file cleanup, and complete admin audit logging.